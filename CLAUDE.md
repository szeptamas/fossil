# Claude Code repository instructions

`AGENTS.md` is the single source of truth for repository guardrails.

Before work:

1. Read `AGENTS.md`.
2. Read `docs/PROJECT-STATE.md`.
3. Load the relevant skill from `.agents/skills/`.
4. Keep the task narrow and the diff minimal.
5. Do not merge unless explicitly instructed.
