# Applications

Each original watch application has its own directory.

Create the first app:

```powershell
.\scripts\new-app.ps1 `
  -Name lion-hello `
  -Identifier lionHelloApp `
  -Title "Lion Hello"
```

Do not edit the SDK example directly.
