# Project state

Last reviewed: 2026-06-17

## Hardware and phone

- Watch: Fossil Hybrid HR Collider
- Phone: Android
- Watch manager: Gadgetbridge
- Working launcher requirement: a Gadgetbridge-generated `customWatchFace` must be active

## Verified toolchain

- Windows host
- Docker Desktop
- Ubuntu 20.04 build container
- Fossil HR SDK: `C:\git\Fossil\Fossil-HR-SDK`
- JerryScript source: `C:\git\Fossil\jerryscript`
- Required JerryScript version: `v2.1.0`
- JerryScript build option: `python3 tools/build.py --jerry-cmdline-snapshot ON`
- Main local repository: `C:\git\Fossil\fossil`
- Raw local knowledge archive: `C:\git\Fossil\_knowledge-archive`

## Verified watch applications

| Identifier | Source | Status |
|---|---|---|
| `snakeApp` | SDK `examples\snake` | Built, installed, and launched successfully |
| `testMenuApp` | SDK `examples\simple-menu` | Built, installed, and launched successfully |

## Verified launcher behavior

Gadgetbridge App Manager writes the requested app name to:

```text
customWatchFace._.config.start_app
```

Stock Fossil watchfaces do not honor this custom launcher path. The active watchface must therefore be generated by Gadgetbridge.

## Repository status

This repository is intended for:

- original watch applications;
- repeatable build/install scripts;
- distilled documentation;
- AI-agent guardrails;
- decisions and evidence.

Upstream repositories and the raw knowledge archive stay outside this repository.

## Current next milestone

Create and validate the first original application:

```text
lionHelloApp
```

The first milestone is complete only after:

1. source is created under `apps\lion-hello`;
2. Docker build creates `dist\lionHelloApp.wapp`;
3. Gadgetbridge installs it;
4. it launches on the real watch;
5. the result is recorded in `docs/APP-CATALOG.md`.
